<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ricardopazdemiquel.plataformanur">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Utiles.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_nur_square"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_nur_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".Carga">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login2"></activity>
        <activity
            android:name=".TabBarActivity"
            android:label="@string/title_activity_tab_bar" />
        <activity android:name=".DetalleMateriaActivity" />
        <activity android:name=".HistorialAcademico"></activity>
    </application>

</manifest>